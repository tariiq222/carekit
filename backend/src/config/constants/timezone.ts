/**
 * L2: Clinic timezone constant — single source of truth.
 * In a multi-tenant setup, this should come from WhiteLabelConfig.timezone per client.
 * For now, all deployments use Asia/Riyadh (Saudi Arabia).
 */
export const CLINIC_TIMEZONE = 'Asia/Riyadh';
