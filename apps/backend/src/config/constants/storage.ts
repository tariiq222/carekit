/**
 * Storage / MinIO constants — single source of truth.
 */

/** Default MinIO bucket name for all uploads. */
export const MINIO_BUCKET = 'carekit';

/** Object path prefixes for organized storage. */
export const UPLOAD_PATH_RECEIPTS = 'receipts';
export const UPLOAD_PATH_KB_FILES = 'kb-files';
