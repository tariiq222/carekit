---
"website": patch
"admin": patch
---

Add official Deqah brand logo (دقة wordmark + blue check) and matching favicon to the public website (sawaa theme navbar) and the super-admin app (sidebar shell + login card). Includes dark and white wordmark variants and Next.js auto-discovered `app/icon.svg` + `app/apple-icon.svg`.
